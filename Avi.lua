           for (int i = 0; i < ?; i++) {
           SetSearchRange(OTHER);
			MemorySearch(logic1, "future", &v4, DWORD);
			MemoryOffset(logic1, "fucker", off[i], &v4, DWORD);
			MemoryWrite(logic1, "fucked", 0, DWORD);
			ClearResults();
			}
